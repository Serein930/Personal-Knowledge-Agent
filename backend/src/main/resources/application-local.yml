spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:postgresql://localhost:5432/agentmind
    username: agentmind
    password: agentmind_dev_password

agentmind:
  agent:
    persistence:
      store: jdbc
  vector-store:
    type: pgvector
    embedding-dimensions: 128
