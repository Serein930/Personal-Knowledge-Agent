# 旧版数据库基线脚本

本目录中的 SQL 仅供已经发布的 `V1__InitializeCoreSchema` Java 迁移使用。
为了保证已有数据库的 Flyway 历史和全新数据库的 `V1` 基线行为一致，这些文件必须保持冻结，不能再追加字段、约束或索引。

从 `V2` 开始，所有数据库变更都必须写入 `db/migration` 下的新版本迁移。
`V4-V7` 已将旧基线的正式结构归并为可审计、可重复执行的 SQL 版本；后续开发只能新增更高版本，禁止修改已经发布的迁移。
